import React from 'react';
import { View, Text } from 'react-native';
import PropTypes from 'prop-types';

import styles from './styles';

const Beer = props => {
  return <Text>This is Beer.</Text>;
};

Beer.propTypes = {};

export default Beer;
