import React from 'react';
import { View, Text } from 'react-native';
import PropTypes from 'prop-types';

import styles from './styles';

const FavBeers = props => {
  return <Text>This is FavBeers.</Text>;
};

FavBeers.propTypes = {};

export default FavBeers;
